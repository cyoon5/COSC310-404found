version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/usr/src/app/data:rw
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped